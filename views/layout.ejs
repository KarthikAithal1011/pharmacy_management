<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title || 'Pharmacy Management' %></title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
  <script src="https://cdn.tailwindcss.com"></script>
  <script>
    tailwind.config = {
      theme: {
        extend: {
          colors: {
            primary: {
              50: '#eff6ff',
              500: '#3b82f6',
              600: '#2563eb',
              700: '#1d4ed8',
            },
            secondary: {
              50: '#faf5ff',
              500: '#a855f7',
              600: '#9333ea',
              700: '#7c3aed',
            }
          },

        }
      }
    }
  </script>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
  <style>
    body {
      font-family: 'Inter', sans-serif; /* A modern, clean sans-serif font */
      font-size: 16px;
      line-height: 1.6;
      color: #333;
    }
    .gradient-bg {
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
    }
    .glass-effect {
      background: rgba(255, 255, 255, 0.95);
      backdrop-filter: blur(10px);
      border: 1px solid rgba(255, 255, 255, 0.2);
    }
    .shadow-modern {
      box-shadow: 0 10px 25px -5px rgba(0, 0, 0, 0.1), 0 10px 10px -5px rgba(0, 0, 0, 0.04);
    }
    .hover-lift {
      transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
    }
    .hover-lift:hover {
      transform: translateY(-2px);
      box-shadow: 0 20px 40px -10px rgba(0, 0, 0, 0.15);
    }

    /* Add a card component for consistent content blocks */
    .card {
      @apply bg-white rounded-lg shadow-modern p-6;
    }

    /* General Form Element Styling (now a class for reuse) */
    .form-input {
      @apply block w-full px-4 py-2 border border-gray-300 rounded-lg shadow-sm focus:ring-primary-500 focus:border-primary-500 sm:text-sm;
      transition: all 0.2s ease-in-out;
    }
    .form-input:focus {
      outline: none;
      border-color: var(--primary-500);
      box-shadow: 0 0 0 3px rgba(59, 130, 246, 0.2); /* Tailwind's ring-primary-500 equivalent */
    }

    /* Button Styles */
    .btn-primary {
      @apply inline-flex items-center justify-center px-4 py-2 border border-transparent text-sm font-medium rounded-md shadow-sm text-white bg-primary-600 hover:bg-primary-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-primary-500 transition-colors duration-200;
    }
    .btn-secondary {
      @apply inline-flex items-center justify-center px-4 py-2 border border-gray-300 text-sm font-medium rounded-md shadow-sm text-gray-700 bg-white hover:bg-gray-50 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-primary-500 transition-colors duration-200;
    }
    .btn-outline-primary {
      @apply inline-flex items-center justify-center px-4 py-2 border border-primary-600 text-sm font-medium rounded-md text-primary-600 bg-transparent hover:bg-primary-50 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-primary-500 transition-colors duration-200;
    }
    .btn-danger {
      @apply inline-flex items-center justify-center px-4 py-2 border border-transparent text-sm font-medium rounded-md shadow-sm text-white bg-red-600 hover:bg-red-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-red-500 transition-colors duration-200;
    }

    /* Adjust font size for different screens if needed */
    @media (max-width: 768px) {
      body {
        font-size: 15px;
      }
    }
    @media (max-width: 480px) {
      body {
        font-size: 14px;
      }
    }
  </style>
</head>
<body class="bg-gradient-to-br from-blue-50 via-white to-purple-50 min-h-screen flex flex-col">
  <!-- Navigation -->
  <% if (typeof showNav !== 'undefined' && showNav) { %>
  <nav class="bg-white shadow-modern border-b border-gray-100 sticky top-0 z-50">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="flex justify-between items-center h-16">
        <div class="flex items-center space-x-4">
          <div class="flex-shrink-0">
            <i class="fas fa-pills text-2xl text-primary-600"></i>
          </div>
          <% if (typeof showHeader !== 'undefined' && showHeader) { %>
          <div class="hidden md:block">
            <div class="ml-10 flex items-baseline space-x-4">
              <h1 class="text-gray-900 px-3 py-2 text-lg font-bold">
                Community Pharmacy
              </h1>
            </div>
          </div>
          <% } %>
        </div>
        <div class="flex items-center space-x-4">
          <% if (typeof showHeader !== 'undefined' && showHeader && typeof username !== 'undefined') { %>
          <a href="/logout" class="bg-red-500 hover:bg-red-600 text-white px-4 py-2 rounded-lg text-sm font-medium transition-colors duration-200">
            <i class="fas fa-sign-out-alt mr-2"></i>Logout
          </a>
          <% } %>
        </div>
      </div>
    </div>
  </nav>
  <% } %>

  <!-- Main Content -->
  <main class="flex-1">
    <%- body %>
  </main>

  <!-- Footer -->
  <% if (typeof showFooter !== 'undefined' && showFooter) { %>
  <footer class="bg-gray-900 text-white mt-auto">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
      <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
        <div class="text-center">
          <div class="flex items-center justify-center space-x-2 mb-4">
            <i class="fas fa-pills text-2xl text-primary-400"></i>
            <span class="text-xl font-bold">Community Pharmacy</span>
          </div>
          <p class="text-gray-400 text-sm">
            Your trusted partner in healthcare. Quality medicines and professional service.
          </p>
        </div>
        <div class="text-center">
          <h3 class="text-lg font-semibold mb-4">Quick Links</h3>
          <ul class="space-y-2 text-sm">
            <li><a href="/menu" class="text-gray-400 hover:text-white transition-colors duration-200">Menu</a></li>
            <li><a href="/view-dashboard" class="text-gray-400 hover:text-white transition-colors duration-200">Shop Medicines</a></li>
            <li><a href="/daily-collection" class="text-gray-400 hover:text-white transition-colors duration-200">Daily Reports</a></li>
          </ul>
        </div>
        <div class="text-center">
          <h3 class="text-lg font-semibold mb-4">Contact Info</h3>
          <div class="space-y-2 text-sm text-gray-400">
            <p><i class="fas fa-map-marker-alt mr-2"></i>123 Health Street, Wellness City</p>
            <p><i class="fas fa-phone mr-2"></i>(123) 456-7890</p>
            <p><i class="fas fa-envelope mr-2"></i>info@pharmacypro.com</p>
          </div>
        </div>
      </div>
      <div class="border-t border-gray-800 mt-8 pt-8 text-center text-sm text-gray-400">
        <p>&copy; 2024 Community Pharmacy. All rights reserved.</p>
      </div>
    </div>
  </footer>
  <% } %>

</body>
</html>
