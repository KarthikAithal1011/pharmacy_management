<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Detailed Sales - Pharmacy Management</title>
  <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@300;400;500;700&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
  <style>
    * {
      box-sizing: border-box;
    }
    body {
      font-family: 'Roboto', sans-serif;
      margin: 0;
      padding: 20px;
      background: white;
      min-height: 100vh;
      color: #374151;
      line-height: 1.6;
    }
    .container {
      max-width: 1200px;
      margin: 0 auto;
    }
    .header {
      display: flex;
      justify-content: space-between;
      align-items: center;
      background: linear-gradient(135deg, rgba(255, 255, 255, 0.95) 0%, rgba(248, 250, 252, 0.95) 100%);
      backdrop-filter: blur(20px);
      padding: 25px 35px;
      border-radius: 20px;
      border: 1px solid #000000;
      box-shadow: 0 20px 40px rgba(0,0,0,0.1), 0 10px 20px rgba(0,0,0,0.05);
      margin-bottom: 35px;
      animation: slideDown 0.6s ease-in-out;
      transition: transform 0.3s ease, box-shadow 0.3s ease;
    }
    .header:hover {
      transform: translateY(-2px);
      box-shadow: 0 25px 50px rgba(0,0,0,0.15), 0 15px 30px rgba(0,0,0,0.1);
    }
    @keyframes slideDown {
      from { opacity: 0; transform: translateY(-20px); }
      to { opacity: 1; transform: translateY(0); }
    }
    h1 {
      color: #1f2937;
      margin: 0;
      font-weight: 800;
      letter-spacing: -0.025em;
      text-shadow: 0 1px 2px rgba(0,0,0,0.1);
    }
    .welcome {
      background: linear-gradient(135deg, #4CAF50 0%, #388E3C 100%);
      color: white;
      padding: 18px 28px;
      border-radius: 30px;
      font-weight: 600;
      box-shadow: 0 8px 25px rgba(76, 175, 80, 0.4);
      transition: transform 0.3s ease, box-shadow 0.3s ease;
    }
    .welcome:hover {
      transform: scale(1.05);
      box-shadow: 0 12px 35px rgba(76, 175, 80, 0.5);
    }
    .main-content {
      background: linear-gradient(135deg, #ffffff 0%, #f8fafc 100%);
      border-radius: 20px;
      border: 1px solid #000000;
      box-shadow: 0 15px 35px rgba(0,0,0,0.08), 0 8px 15px rgba(0,0,0,0.04);
      overflow: hidden;
      animation: fadeIn 0.6s ease-in-out 0.5s both;
      transition: transform 0.3s ease, box-shadow 0.3s ease;
    }
    .main-content:hover {
      transform: translateY(-2px);
      box-shadow: 0 20px 45px rgba(0,0,0,0.12), 0 10px 20px rgba(0,0,0,0.08);
    }
    h2 {
      background: linear-gradient(135deg, #3B82F6 0%, #1D4ED8 100%);
      color: white;
      margin: 0;
      padding: 25px 35px;
      font-weight: 700;
      letter-spacing: -0.025em;
      text-shadow: 0 1px 2px rgba(0,0,0,0.2);
    }
    table {
      width: 100%;
      border-collapse: collapse;
    }
    th, td {
      padding: 18px 22px;
      text-align: left;
      border-bottom: 1px solid #e5e7eb;
    }
    th {
      background: linear-gradient(135deg, #f9fafb 0%, #f3f4f6 100%);
      color: #374151;
      font-weight: 700;
      font-size: 14px;
      text-transform: uppercase;
      letter-spacing: 0.75px;
    }
    tr:hover {
      background: linear-gradient(135deg, #f9fafb 0%, #f3f4f6 100%);
      transition: background 0.3s ease, transform 0.2s ease;
      transform: scale(1.01);
    }
    .logout {
      background: linear-gradient(135deg, #ffffff 0%, #f9fafb 100%);
      padding: 25px;
      border-radius: 20px;
      border: 1px solid #000000;
      box-shadow: 0 10px 25px rgba(0,0,0,0.08), 0 5px 10px rgba(0,0,0,0.04);
      text-align: center;
      animation: fadeIn 0.6s ease-in-out 0.9s both;
      transition: transform 0.3s ease, box-shadow 0.3s ease;
    }
    .logout:hover {
      transform: translateY(-2px);
      box-shadow: 0 15px 35px rgba(0,0,0,0.12), 0 8px 15px rgba(0,0,0,0.08);
    }
    .logout a {
      display: inline-block;
      padding: 15px 28px;
      background: linear-gradient(135deg, #4CAF50 0%, #388E3C 100%);
      color: white;
      text-decoration: none;
      border-radius: 30px;
      font-weight: 600;
      transition: all 0.3s ease;
      box-shadow: 0 4px 12px rgba(76, 175, 80, 0.3);
    }
    .logout a:hover {
      transform: translateY(-3px) scale(1.02);
      box-shadow: 0 10px 25px rgba(76, 175, 80, 0.4);
    }
    .logout a:active {
      transform: translateY(-1px) scale(0.98);
    }
    @keyframes fadeIn {
      from { opacity: 0; }
      to { opacity: 1; }
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="header">
      <h1><i class="fas fa-chart-line"></i> Detailed Sales</h1>
      <div class="welcome">
        <i class="fas fa-user"></i> Welcome, <%= username %>!
      </div>
    </div>

    <div class="main-content">
      <h2><i class="fas fa-receipt"></i> Customer Transactions</h2>
      <table>
        <thead>
          <tr>
            <th><i class="fas fa-hashtag"></i> Receipt Number</th>
            <th><i class="fas fa-user"></i> Customer Name</th>
            <th><i class="fas fa-calendar-alt"></i> Date</th>
            <th><i class="fas fa-rupee-sign"></i> Total Amount</th>
          </tr>
        </thead>
        <tbody>
          <% if (transactions && transactions.length > 0) { %>
            <% transactions.forEach(transaction => { %>
              <tr>
                <td><%= transaction.receipt_id %></td>
                <td>Walk-in Customer</td>
                <td><%= new Date(transaction.date).toLocaleDateString() %></td>
                <td>₹<%= transaction.total_after_discount %></td>
              </tr>
            <% }); %>
          <% } else { %>
            <tr>
              <td colspan="4" style="text-align: center; padding: 40px; color: #666;">No sales data available</td>
            </tr>
          <% } %>
        </tbody>
      </table>
    </div>

    <div class="logout">
      <a href="/menu">
        <i class="fas fa-arrow-left"></i> Back to Menu
      </a>
    </div>
  </div>
</body>
</html>
